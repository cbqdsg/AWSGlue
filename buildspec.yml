version: 0.2
env:
  variables:
    TEMPLATE_BUCKET: "aws-glue-scripts-634486747428-ap-northeast-1"
phases:
  build:
    commands:
      - aws cloudformation package --template CFGlue.yaml --s3-bucket aws-glue-scripts-634486747428-ap-northeast-1/ --output-template-file CFGlue.yaml
artifacts:
  files:
    - 'CFGlue.yaml'
    